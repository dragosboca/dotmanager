require 'spec_helper'

describe Dotmanager do
  it 'has a version number' do
    expect(Dotmanager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
